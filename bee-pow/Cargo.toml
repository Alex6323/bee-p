[package]
name = "pow"
version = "0.1.0"
authors = ["Bee developers"]
edition = "2018"

[dependencies]
common = { path = "../bee-common" }
crypto = { path = "../bee-crypto" }

num_cpus = "1.11.1"
crossbeam = "0.7.3"
