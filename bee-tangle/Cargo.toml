[package]
name = "bee-tangle"
version = "0.1.0"
authors = ["Bee developers"]
edition = "2018"

[dependencies]
bee-bundle = { path = "../bee-bundle" }
dashmap = "3.7"
async-std = { version = "1.5", features = ["unstable"] }
