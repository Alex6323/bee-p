[package]
name = "bee-crypto"
version = "0.1.0"
authors = ["Bee developers"]
edition = "2018"

[dependencies]
bee-ternary = { path = '../bee-ternary' }
tiny-keccak = { version = "2.0.1", features = ["keccak"] }
