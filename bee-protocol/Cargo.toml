[package]
name = "bee-protocol"
version = "0.1.0"
authors = ["Bee developers"]
edition = "2018"

[dependencies]
bee-bundle = { path = "../bee-bundle" }
bee-common = { path = "../bee-common" }

async-std = "1.5.0"
futures = "0.3.4"

[dev-dependencies]
bee-test = { path = "../bee-test" }
