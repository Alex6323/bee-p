[package]
name = "bee-protocol"
version = "0.1.0"
authors = ["Bee developers"]
edition = "2018"

[dependencies]
bee-bundle = { path = "../bee-bundle" }

[dev-dependencies]
futures = "0.3.4"
bee-test = { path = "../bee-test" }
